<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /WebNote/

  # if the request is not a file or directory...
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # ...send it to index.php, preserving the query string
  RewriteRule ^ index.php [QSA,L]
</IfModule>
